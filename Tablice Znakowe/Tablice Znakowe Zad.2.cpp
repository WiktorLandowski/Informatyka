#include<iostream>
using namespace std;

int main(){
char text[201];
cout << "Wprowadz tekst (maks 200 znakow): ";
cin.getline(text, 201);
for (int i=0; text[i] != '\0'; i++){
	if (text[i] == ' ' = '-') (text[i] == '.' = '-');{
			}
cout << "Zmieniony tekst: " << text[i] << endl;
return 0;
}
}